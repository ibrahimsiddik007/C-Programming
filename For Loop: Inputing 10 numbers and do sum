#include<stdio.h>
int main()
{
    
    int i,n,sum = 0;

    printf("Enter the 10 number: ");
    for (i=1;i<=10;i++)
    {   
        
       printf("Number - %d : ",i);
       scanf("%d",&n);
        sum = sum + n;
        
    }

        printf("The sum is: %d",sum);
    return 0;
}
