#include<stdio.h>
int main()

{
    int numberoftomatoes = 20;
    int minimumtomatoes = 8;
    if( (numberoftomatoes>=8) && (minimumtomatoes == 8) ){
        printf("\n");
        printf("You are a pro picker!");
    }
     printf("\n");
     return 0;
}
/*We can also link more than two condition between the if variable.This can be added according users need.*/
