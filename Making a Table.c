#include<stdio.h>
#include<stdlib.h>
#include<ctype.h>
#include<string.h>
#include<math.h>
int main()
{
    int columns;
    int rows;
    for(rows=1; rows<=10; rows++ ){

        for(columns=1; columns<=20; columns++){
            printf("%d ",columns);
    }

         printf("\n");

    }

    return 0;
}

//This code is used to create matrix and do some basic works.

